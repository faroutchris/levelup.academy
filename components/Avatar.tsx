import { styled } from '../config/stitches.config';

const ContentMain = styled('img', {
  width: 48,
  height: 48,
  borderRadius: '100%',
  boxShadow: '0 2px 5px rgba(0,0,0,0.15)',
});

export default ContentMain;
