import { styled } from '../config/stitches.config';

const Card = styled('div', {
  display: 'block',
  backgroundColor: '$white',
  borderRadius: '$triple',
  padding: '$lg',
});

export default Card;
